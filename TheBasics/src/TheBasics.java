/**
 * The first assignment of our summer coding adventure. This will get you used to function return
 * values and print statements.
 * 
 * @author Stephen Crowell and <<YOU>>
 *
 */
public class TheBasics {
	
	/**
	 * Check the test file to see what you need to fix this one. Don't edit the test file, change
	 * the function.
	 */
	public static int test() {
		//TODO this one 
		return 0;
	}
	
	/**
	 * The function practices using System.out.println() with strings. You want to print first, a
	 * space, second, and a period.
	 * @param first
	 * @param second
	 */
	public static void printLinePractice(String first, String second) {
		//TODO this one.
	}
	
	/**
	 * The function practices using System.out.println() with strings. You want to print first, a
	 * space, second, and a period. For this one, you will need a new line character.
	 * @param first
	 * @param second
	 */
	public static void printfPractice(String first, String second) {
		//TODO this one.
	}
	
	/**
	 * The function compares two integers. If one is greater than two, it returns 1. If two is
	 * greater, it returns -1. If one equals two, return 0. Do not use any built in methods.
	 * @param first
	 * @param second
	 */
	public static int compare(int one, int two) {
		//TODO this one.
		return 0;
	}
}
